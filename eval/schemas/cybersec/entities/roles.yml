name: Roles
type: fact_table
table: roles
grain: one row per role record
description: Auto-profiled schema for roles (5 rows). Contains 3 columns.
dimensions:
  - name: id
    sql: id
    type: number
    description: Primary key
    primary_key: true
    unique_count: 5
    sample_values:
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
  - name: name
    sql: name
    type: string
    unique_count: 5
    sample_values:
      - admin
      - analyst
      - auditor
      - responder
      - viewer
  - name: description
    sql: description
    type: string
    unique_count: 5
    sample_values:
      - Compliance and audit access
      - Full organization access
      - Incident response and alert management
      - Read-only dashboard access
      - View and query access, can manage scans
measures:
  - name: role_count
    sql: id
    type: count_distinct
use_cases:
  - Avoid for row-level team_memberships queries — use those entities directly
