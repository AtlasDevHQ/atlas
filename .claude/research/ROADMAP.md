# Atlas Roadmap

> Public repo: [AtlasDevHQ/atlas](https://github.com/AtlasDevHQ/atlas). Tracking lives in [GitHub Issues](https://github.com/AtlasDevHQ/atlas/issues) and [Milestones](https://github.com/AtlasDevHQ/atlas/milestones).
>
> Previous internal milestones (v0.1–v1.3) archived in `ROADMAP-archive.md`.
>
> **Versioning**: Public semver starts at 0.0.x. Internal milestones (v0.1–v1.3) were pre-public. The numbers below are public semver. 1.0.0 is the hosted SaaS launch at app.useatlas.dev.
>
> **License**: AGPL-3.0. The hosted SaaS is the primary commercial offering. Commercial embedding requires a separate license.

---

## North Star: 1.0.0 — SaaS Launch

Atlas 1.0.0 = hosted SaaS at **app.useatlas.dev**. Target: later 2026. Every milestone below is a stepping stone toward a production multi-tenant SaaS that teams sign up for, connect their database, and start querying — no deploy step.

The AGPL license makes the SaaS the natural path for commercial users. Self-hosted remains free and fully functional. The hosted product adds managed infrastructure, onboarding, monitoring, and support.

---

## Shipped

<details>
<summary>Work completed since public repo launch (click to expand)</summary>

### Public Launch (#1–#10)
- [x] Initial public release
- [x] Admin user management + default password enforcement (#1)
- [x] Vercel deploy button with Neon + AI Gateway (#2, #3)
- [x] `@useatlas` packages published to npm (0.0.2)
- [x] CI: template drift check (#9, #10)
- [x] CI: automate starter repo sync (#7, #8, #34, #35)

### Adapter Plugin Refactor (#11–#32)
- [x] Plugin SDK: `parserDialect` and `forbiddenPatterns` on datasource plugins (#14, #23)
- [x] `validateSQL` and `ConnectionRegistry` made plugin-aware (#15, #25)
- [x] Agent dialect system made plugin-driven (#16, #24)
- [x] ClickHouse, Snowflake, DuckDB, Salesforce plugins created (#17–#20, #26–#28, #31)
- [x] Adapter code stripped from core — plugins own their adapters (#21, #32)
- [x] Adapter tests moved to plugins (#22)
- [x] Fix: anchor forbidden patterns to avoid false positives (#29, #30)

### Starter Automation (#33–#38)
- [x] Platform-specific READMEs with deploy buttons (#12, #33)
- [x] Sync starters post adapter strip (#36, #37, #38)

### Python Data Science Sandbox (#39–#51)
- [x] `executePython` tool with import guard (#43, #46)
- [x] Sidecar Python backend (#40, #47)
- [x] Chart rendering in chat UI (#41, #48)
- [x] Agent prompt tuning for Python tool usage (#44, #49)
- [x] nsjail Python sandbox backend (#42, #50)
- [x] Vercel sandbox Python backend (#45, #51)

### Infra & Cleanup (#52–#55)
- [x] Fix missing deps and files in starter templates (#52)
- [x] Drop Render as deploy target (#53, #54)
- [x] Sandbox architecture design doc (#55)

### 0.1.0 — Documentation & Developer Experience
- [x] Docs site (docs.useatlas.dev) — Fumadocs, API reference from OpenAPI, 24+ pages
- [x] DX polish — `atlas doctor`, `atlas validate`, shell completions, better first-run errors
- [x] Test coverage — web UI, SDK integration, E2E expansion
- [x] Project hygiene — CHANGELOG, CONTRIBUTING, issue/PR templates, brand unification

### 0.2.0 — Plugin Ecosystem
- [x] Plugin SDK stable — `definePlugin`, Zod config schemas, multi-type plugins, testing utilities
- [x] 18 npm packages published under `@useatlas/*` scope
- [x] Plugin scaffold (`bun create @useatlas/plugin`), cookbook, composition docs, health checks

### 0.3.0 — Admin Console & Operations
- [x] Admin phase 2 — connections, users, plugins, settings with live runtime wiring
- [x] Observability — query analytics, token usage, OpenTelemetry traces, health dashboard
- [x] Scheduled tasks v2 — create/edit UI, history, delivery channels

### 0.4.0 — Chat Experience
- [x] Chat polish — theming, follow-ups, Excel export, mobile-responsive, saved queries
- [x] Discovery — visual schema explorer, area/stacked bar/scatter charts

### 0.5.0 — Launch
- [x] Embeddable widget — `@useatlas/react`, widget host, script tag loader, programmatic JS API
- [x] Distribution — BigQuery plugin, conversation sharing with OG tags and embed mode
- [x] SDK streaming — `streamQuery()` async iterator with abort support
- [x] Launch prep — onboarding hardening, README overhaul, landing page refresh, launch content
- [x] Quality — `@useatlas/types` extraction, error handling hardening, CLI test fixes

### 0.5.1 — Agent-Friendly Docs
- [x] Frances Liu docs framework — tutorials, how-to guides, reference, explanation pages
- [x] 119 MDX pages audited for agent optimization, llms.txt

### 0.5.2 — Onboarding & CLI Polish
- [x] Pattern-matched errors, progress indicators, CLI help, first-run detection
- [x] Pool exhaustion handling, schema suggestions, transient vs permanent error hints

### 0.5.3 — UI & Accessibility Polish
- [x] ARIA, Lighthouse audit, keyboard navigation, error boundaries
- [x] Loading states, empty states, mobile polish, chart responsiveness
- [x] Widget types, error states, CSS customization docs

### 0.5.4 — SDK & Integration Polish
- [x] SDK `listTables()`, error code catalog, streaming docs
- [x] Integration tests (widget, SDK, MCP), docs cross-cuts, Obsidian plugin

</details>

---

## Shipped Milestones (0.6.0 → 1.2.2)

Full detail archived in [`ROADMAP-archive.md`](./ROADMAP-archive.md). Issues + PR bodies remain the source of truth; one-liners below are hooks, not commitments.

- [x] **0.6.0 — Governance & Operational Hardening** (#7, 44 issues) — action timeout + rollback, configurable step/import limits, RLS multi-column/array/OR, session management, audit CSV + search + classification, typed plugin peer deps, tool hooks, custom SQL validation, semantic diff UI, Teams + webhook + email-digest plugins.
- [x] **0.7.0 — Performance & Multi-Tenancy** (#8, 27 issues) — Better Auth org plugin, tenant-scoped connection pooling, query result caching, semantic layer indexing, streaming Python, `atlas learn` CLI.
- [x] **0.7.x Refinement Arc** (#15–#19) — five point releases across cleanup, type safety, error handling, test hardening, docs completeness.
- [x] **0.8.0 — Intelligence & Learning** (#9, 12 issues) — dynamic learning (proposals + injection + admin UI), self-hosted models, auth refactor, prompt library, query suggestions, notebook UI phase 1.
- [x] **0.8.1 — Notebook Refinement** (#20, 36 issues) — fork/branch + DnD reorder + markdown cells + Markdown/HTML export, keyboard nav tests, error-code docs.
- [x] **0.9.0 — SaaS Infrastructure** (#21, 86 issues) — tenant provisioning, billing, enterprise auth (SSO/SCIM/IP/custom roles/approval), compliance (audit retention/PII/reporting), 8-platform Chat SDK, platform ops (SLA/abuse/backups/residency), onboarding.
- [x] **0.9.1 — Docs & Polish** (#22, 94 issues) — docs for every SaaS feature, OpenAPI auto-gen (4,300 → 230 lines), enterprise hardening, 8 architecture refactors, react-hook-form + `useAdminMutation` across all 26 admin pages.
- [x] **0.9.2 — Docs Persona Audit** (#23, 8 issues) — classified all 354 docs pages by persona; reframed deployment + enterprise guides.
- [x] **0.9.3 — Architecture Deepening** (#25, 6 + 2 superseded) — 13 architecture-wins entries: auth error dedup, `extractFetchError`, `AdminContentWrapper` (-302 lines), route error wrapper (-852 lines), `ResultCardBase`, OpenAPI schema factories, conversation fetch client.
- [x] **0.9.4 — Effect.ts Migration** (#26, 23 issues) — every backend service became a `Context.Tag`, `@effect/ai` + `@effect/sql` adopted across the API server, all route handlers migrated from `c.get()` to Effect Context.
- [x] **0.9.5 — Post-Effect Validation** (#27, 7 + 1 deferred) — no regressions: 250 unit + 434 EE + 44 browser tests green; 3 env-isolation PRs.
- [x] **0.9.6 — SaaS Customer Experience** (#28, 24 issues) — org-context enforcement on every route, workspace-level settings overrides, API key management UI, integrations hub, custom domain, billing, per-workspace sandbox + residency, Drizzle Kit migrations.
- [x] **0.9.7 — SaaS-First Admin Experience** (#29, 53 issues) — deploy mode flag, hot-reloadable settings, OAuth-first connect flows for 7 platforms, plugin marketplace, semantic layer web editor, BYOT dual-mode, deploy validation CI, sandbox BYOC library.
- [x] **0.9.8 — Docs & Polish** (#30, 27 issues) — docs for 0.9.7, integration type safety, data residency (signup + migration phases 1–2), periodic settings refresh, deploy-validation CI fixes, npm publishing fixes.
- [x] **1.0.0 — SaaS Launch** (#24, 40 issues) — 3 regions live (US/EU/APAC), cross-region migration, pre-launch smoke test, competitive refresh, legal pages, SLA, OpenStatus, hosted docs.
- [x] **1.1.0 — Notebook Evolution** (#33, 11 issues) — chat-to-notebook, dashboard bridge, report route, execution metadata, fork UX ("What if?" button + gutter indicators), `transformMessages` extraction.
- [x] **1.2.0 — Developer/Published Mode** (#34, 31 issues) — draft/published content model, mode middleware, overlay queries, atomic publish endpoint, agent isolation, connect redesign, `__demo__` onboarding identity.
- [x] **1.2.1 — Adaptive Starter Prompts** (#35, 15 issues) — adaptive starter surface end-to-end with favorites + popular + library + cold-start, admin moderation, `atlas learn` CLI, widget + SDK + notebook surfaces, mode participation.
- [x] **1.2.2 — Admin Console Polish & Schema Consolidation** (#36, 70+ issues) — admin final-pass buckets 1+2+4, `@useatlas/schemas` wire consolidation (`admin-schemas.ts` 542 → 241 lines), `@useatlas/types` 0.0.11 → 0.0.14 with `Percentage` / `Ratio` branded types + 4 discriminated-union migrations, `MutationErrorSurface` + branded `FeatureName` registry across ~40 admin pages.

---

## 1.2.3 — Security Sweep

Full security audit with in-milestone remediation. Audit-and-fix shape: P0/P1/P2 findings fixed in-milestone, P3s punted to a cleanup tail. Findings captured in `.claude/research/security-audit-1-2-3.md`. Tracking issue #1718. Ships before 1.3.0.

- [x] Phase 1 — Audit Better Auth config + middleware coverage (#1720, PR #1734). All P0/P1/P2 findings fixed: F-01 → PR #1738 (+ #1742 for dashboards twin #1736, + #1749 for DB invariant #1737 which also revokes drifted share_tokens); F-02 → PR #1735; F-03 → PR #1744; F-04 → PR #1748; F-05 bundled into F-06; F-06 → PR #1739; F-07 → PR #1747. Follow-ups shipped in milestone: share-token redaction (#1743 → PR #1771, F-01 cluster hardening), Better Auth rate-limit integration test (#1741 → PR #1793, F-06 wiring coverage — extracted `buildAuthOptions(deps)` helper + three-test pack: config wiring snapshot, live 10×401/1×429 loop, signup enumeration parity; surfaced incidental P3 #1792 for upstream `image: null` asymmetry — shipped PR #1838, signup now normalizes `image` across new vs existing paths)
- [x] Phase 2 — Audit org-scoping on every route + ContentMode consumers (#1721). Audit shipped (PR #1757, findings F-08..F-16). All P0/P1/P2 findings fixed: F-08 `/admin/organizations/**` → PR #1762 (#1750); F-09 `/admin/abuse/**` → PR #1763 (#1751); F-10 `platform_admin` escalation via role + invite routes → PR #1758 (#1752); F-11 conversation CRUD by :id filters by user_id only → PR #1769 (#1753); F-12 pending-action CRUD filters by requested_by only → PR #1769 (#1754); F-13 `expireStaleRequests` global TTL sweep → PR #1770 (#1755); F-14 workspace-admin user-ban escapes workspace → PR #1770 (#1756) — also added last-admin guard on /membership + `logAdminAction` audit trail across ban/unban/removeMembership. F-15 and F-16 remain in the P3 cleanup tail. Also resolved flaky isolation test #1768 (EE IP-allowlist mock). Infra unblocks: PR #1759 + #1760 (bun.lock / Railway watchPatterns drift); PR #1764 (scaffold publish-race + missing cli/lib sync, `@useatlas/types` 0.0.15 + template ref bump)
- [x] Phase 3 — Audit SQL validation edges + fuzz the 4-layer validator (#1722, PR #1775, findings F-17..F-21). All P1/P2 fixes shipped in PR #1776 (closes #1772 / #1773 / #1774): F-17 `unwrapMysqlExecutableComments()` unwraps `/*!NNNNN ... */` before validation layers (Option A, 8 regression pins covering variant matrix + PG no-op); F-18 AST guard rejects `stmt.into?.type === "into"` when `keyword !== "var"` (excludes MySQL `@var` session assignment); F-19 `FORBIDDEN_PATTERNS` extended to `/\bINTO\s+(?:OUTFILE|DUMPFILE)\b/i`. F-20 (quoted-identifier case-fold) + F-21 (dialect dangerous functions) stay in P3 tail. Regression pins F-17.a–F-17.h, F-18, F-19 kept in the fuzz suite so a future refactor that reopens any bypass turns the suite red
- [x] Phase 4 — Audit audit-log coverage on write routes (#1723, PR #1794). Audit complete (201 write routes across 52 files enumerated): 15 findings filed (F-22..F-36) covering plugin install/uninstall (#1777 F-22 P0), SCIM connection + group-mapping (#1778 F-23 P0), IP allowlist add/remove (#1779 F-24 P0), EE custom-role CRUD + assignment (#1780 F-25 P0), audit retention config + manual purge + hard-delete (#1781 F-26 P0), EE purge scheduler + retention mutations (#1782 F-27 P1), admin session revocation (#1783 F-28 P1), partially-audited admin subrouters (#1784 F-29 P2), BYOT credential management — email provider + LLM model config (#1785 F-30 P1), admin-orgs.ts platform-gated post-F-08 but still unaudited (#1786 F-31 P1), workspace enterprise config writes — domains/branding/residency/compliance (#1787 F-32 P1), abuse reinstate split audit trail (#1788 F-33 P2), wizard onboarding + connection test paths bypass connection.create (#1789 F-34 P2), prompt library + semantic-improve + starter-prompt moderation writes unaudited (#1790 F-35 P2), admin_action_log retention policy gap (#1791 F-36 P2). **All 5 P0s + all 5 P1s + 5/5 P2s shipped (15/15 total; F-36 Phase 1 data layer only, Phase 2 admin UI tracked as #1813):** F-22 plugins → PR #1802 (#1777, `plugin.install`/`uninstall`/`enable`/`disable`/`config_update` + `plugin.catalog_create`/`update`/`delete` + bonus `plugin.catalog_cascade_uninstall` for transactional cascade; `keysChanged: string[]` keeps config values out of metadata); F-23 SCIM → PR #1796 (#1778, `scim.connection_delete`/`group_mapping_create`/`group_mapping_delete` with bearer-token key-absent assertions + credential scrubbing on failure); F-24 IP allowlist → PR #1797 (#1779, `ip_allowlist.add`/`remove` + pre-delete CIDR capture + failure-path emission); F-25 EE custom roles → PR #1800 (#1780, `role.create`/`update`/`delete`/`assign` with pre-fetch for `previousRole`/`previousPermissions`; new `getRoleByName()` helper in `ee/src/auth/roles.ts` resolves mutable role name → stable ID for audit); F-26 audit-retention → PR #1799 (#1781, 4 entries for `policy_update`/`export`/`manual_purge`/`manual_hard_delete` + previous-policy snapshot + explicit `not.toHaveProperty` assertions keeping exported rows out of metadata); F-28 session revocation → PR #1801 (#1783, `user.session_revoke`/`session_revoke_all` across both admin-sessions.ts routes + `admin.ts::revokeUserSessionsRoute`; pre-fetch for `targetUserId` + explicit key-absence assertions on session tokens); F-30 BYOT credentials → PR #1805 (#1785, `email_provider.update`/`delete`/`test` + `model_config.update`/`delete`/`test` across both admin-email-provider.ts and admin-model-config.ts; `hasSecret: true` marker keeps API keys / SMTP passwords out of metadata; failure-path emission on test routes closes the credential-oracle gap); F-31 admin-orgs drift → PR #1804 (#1786, option b shipped — no new `ADMIN_ACTIONS` entries, reuses canonical `workspace.suspend`/`unsuspend`/`change_plan`/`delete` with `scope: "platform"` matching platform-admin.ts verbatim; parity regression test compares audit rows across both surfaces directly so a one-sided drift turns red); F-32 workspace enterprise config → PR #1806 (#1787, 12 routes across 4 admin files — `domain.workspace_register`/`workspace_remove`/`workspace_verify`/`workspace_verify_dns`, `branding.update`/`delete`, `residency.workspace_assign` with explicit `permanent: true` + `migration_request`/`migration_retry`/`migration_cancel`, `compliance.pii_config_update`/`pii_config_delete` deliberately named distinct from `audit_retention.*` so compliance review can distinguish PII-masking enforcement from retention windows); F-27 EE purge scheduler → PR #1807 (#1782, introduces `system:audit-purge-scheduler` / `system:audit-retention` actor convention in `logAdminAction` — scheduler + `setRetentionPolicy` + `hardDeleteExpired` now emit self-audit rows; `audit_log.purge_cycle` / `audit_retention.policy_update` / `audit_retention.hard_delete` added to `ADMIN_ACTIONS`; zero-row cycles still emit for scheduler-health evidence; unblocks F-29's `schedule.tick` handler); F-33 abuse reinstate → PR #1808 (#1788, dual-write `workspace.reinstate_abuse` alongside existing `abuse_events` row so compliance queries over `admin_action_log` catch reinstate actions; `!hasInternalDB()` fallback no longer silently succeeds — it now attempts the audit write best-effort and flags skip state in the response); F-35 content-governance trio → PR #1809 (#1790, 15 write routes across `admin-prompts.ts` / `admin-semantic-improve.ts` / `admin-starter-prompts.ts` emit `prompt.*` / `semantic.improve_*` / `starter_prompt.*` actions; mirrors the learned-patterns pattern — cut-and-paste at scale); F-29 + F-34 admin-subrouter + wizard audit coverage → batched (#1784 + #1789, 5 primary subrouters — `admin-sso.ts`, `admin-connections.ts`, `scheduled-tasks.ts`, `admin-approval.ts`, `admin.ts` — + `wizard.ts` `/save` onboarding path. New `ADMIN_ACTIONS` entries: `sso.verify_domain` / `sso.enforcement_update` (prompt typo'd `group_mapping_update` — that route lives in `admin-scim.ts` which F-23 already covered, corrected to `enforcement_update` matching the actual gap), `connection.probe` (ephemeral URL probes on `POST /test`) + `connection.health_check` (registered-conn routine checks on `POST /:id/test`) split into two distinct action types per type-design review so forensic queries separately count the privilege-escalation probe surface vs. routine operational signal without parsing a metadata discriminator (matches the `manualHardDelete` vs `hardDelete` + `archive` vs `archiveReconcile` pattern already in the catalog), `connection.pool_drain` (platform scope, org-wide only — per-connection `POST /:id/drain` stays open as F-29 residual), `schedule.trigger` / `schedule.preview` / `schedule.tick` (tick uses the F-27 `system:scheduler` reserved actor — pinned by exact string in the regression test — and emits even at zero tasks so absence is the signal that the scheduler stopped), `approval.rule_create` / `rule_update` / `rule_delete` / `expire_sweep` (rule_update metadata carries `keysChanged: string[]` — never pattern / threshold values since those may be sensitive shape data for a compromised admin), `user.password_change` (self-action, `targetId: actorId` pinned in regression test; metadata never includes password material — assertion enforces no `currentPassword`/`newPassword` substring; emitted BEFORE the post-Better-Auth flag-clear so a flag-clear DB failure doesn't lose the audit row or confuse the user), `semantic.bulk_import` (one row per call, not per entity — `sourceRef` narrows to `disk:<connectionId>` when the caller scopes). Wizard parity regression test in `admin-wizard-save-audit.test.ts` calls both `admin-connections` POST / and wizard `/save` for the same payload and asserts structural equality of the emitted `connection.create` rows — one-sided drift breaks the suite. Per-file audit tests in `admin-sso-audit.test.ts` / `admin-connections-audit.test.ts` / `scheduled-tasks-audit.test.ts` / `admin-approval-audit.test.ts` / `admin-password-semantic-import-audit.test.ts`. Multi-agent review pass added: silent-failure-hunter flagged the password flag-clear gap (fixed), comment-analyzer caught a dead `POST /connection-test` reference in the wizard comment + "this PR" language rot (both tightened), type-design-analyzer prompted the probe/health-check split (shipped)). Shared `packages/api/src/lib/audit/error-scrub.ts` landed with F-28 — `errorMessage()` scrubs `postgres://user:pass@` URIs + truncates to 512 chars; `causeToError()` walks `Effect.Cause` to extract underlying error (returns `undefined` on pure interrupt so callers can skip audit rows for client-disconnect). **F-36 #1791 Phase 1 shipped** — PR #1821 landed `.claude/research/design/admin-action-log-retention.md` design doc (option 1 NULL-scrub + 7-year default + out-of-band pino boundary + parallel retention-config table), migration `0035_admin_action_retention.sql` (new `admin_action_retention_config` table + `admin_action_log.anonymized_at` column + partial index), `purgeAdminActionExpired` / `anonymizeUserAdminActions` in `ee/src/audit/retention.ts`, and two-branch scheduler cycle (`admin_action_log.purge_cycle` row per tick, independent of audit-log branch). Phase 2 admin UI shipped → PR #1839 (#1813, `/admin/audit/retention` split-panel for audit-log and admin-action-log policies + export + hard-delete; session-cached \`useAdminFetch\` + \`useAdminMutation\`; unblock follow-up #1841 tracks OpenAPI regen + \`@useatlas/types\` 0.0.16 publish). **F-29 residuals shipped (#1828)** — bundled with F-46 in the same PR since both are audit-metadata hygiene on `admin-integrations.ts` and three other routes. 4 new audit-emission sites: `admin-integrations.ts` `POST /email/test` orphan (new `integration.test` action — exercises a stored credential, treated as a credential oracle consistent with `email_provider.test`), `admin-invitations.ts` `DELETE /users/invitations/:id` (new `user.revoke_invitation` action with pre-fetched `invitedEmail` + `role` + `previousStatus` so the forensic context survives even if the invitations table is later retention-purged), `platform-sla.ts` `POST /evaluate` (new `sla.evaluate` action at platform scope, metadata carries only `newAlertCount` — alert payloads are PII-adjacent; `tapError` emits failure-status row when the pipeline rejects), `admin-connections.ts` `POST /:id/drain` (reuses the existing `connection.pool_drain` action; disambiguated from the org-wide drain shipped in #1823 by `scope: "workspace"` + `targetId: connectionId`, failure-path emission on throw so a mid-flight pool-drain error still lands a forensic row). Per-file regression tests: `admin-integrations-audit.test.ts`, `admin-invitations-audit.test.ts`, `platform-sla-audit.test.ts`, plus an extension to `admin-connections-audit.test.ts` covering the per-id path. Incidentals surfaced: P3 #1798 (`IPAllowlistError.code` never reaches HTTP status mapping — `Effect.runPromise` flattens tagged errors through `FiberFailure`, so 400/409/404 falls through to 500; closed as deferred). CI gate: F-28 added `maxLength: 255` to session path-param schemas without running `openapi:extract` — main turned red briefly; fixed in PR #1803 (spec-only regeneration, 2 lines)
- [x] Phase 5 — Audit secret/error surfaces + plugin credential storage (#1724). Audit complete (52 files / every route 500-body + 10 integration stores + 2 plugin tables walked): 6 P1/P2 issues filed (F-41–F-44, F-46, F-47 — F-45 duplicate-helper consolidation folded into F-44's fix) covering workspace integration tokens stored plaintext (#1815 F-41 P1 → PR #1833, plaintext-drop follow-up #1832), plugin config plaintext JSONB (#1816 F-42 P1), marketplace `GET /available` returns `installedConfig` raw without secret mask (#1817 F-43 P1), pino error logs don't scrub driver-echoed connection-string userinfo via shared `errorMessage()` (#1818 F-44 P2 — F-45 duplicate-helper consolidation folded in), BYOT integration audit metadata lacks `hasSecret: true` marker vs. F-30 precedent (#1819 F-46 P2), no key-rotation path for `ATLAS_ENCRYPTION_KEY` + `BETTER_AUTH_SECRET` fallback entangles session-signing with at-rest encryption (#1820 F-47 P2). **No P0.** Affirmative-verification rows: widget bundle clean (F-48), platform ANTHROPIC/OPENAI keys env-only never persisted, `workspace_model_config.api_key_encrypted` already uses `encryptUrl`. P3 notes in the audit doc only: F-49 (scope doc-fix — `lib/effect/classify.ts` doesn't exist; code is in `hono.ts`), F-50 (`shared/lib.ts` reads non-`NEXT_PUBLIC_*` env — runtime-safe, hygiene only), F-51 (`custom_domains.verification_token` plaintext — not a credential), F-52 (`Effect.runPromise` + FiberFailure unwrap — systematic-risk pattern documented for future contributors, only IP-allowlist list path remains; add/delete migrated off via #1798 fix). Remediation clusters: F-43 first (live disclosure, ~20 lines); then F-41+F-42+F-46 (credential encryption + hasSecret marker, same handlers); then F-44+F-45 (pino scrubber + helper consolidation); then F-47 (rotation runbook + key-versioning migration, standalone). **F-43 → PR #1830** (#1817, marketplace `GET /available` now masks secret fields via new shared `packages/api/src/lib/plugins/secrets.ts` — `MASKED_PLACEHOLDER` + `maskSecretFields` + `restoreMaskedSecrets` + `parseConfigSchema` — mirroring the admin-plugins platform surface; marketplace `PUT /:id/config` gained a pre-fetch + placeholder restore guard so the admin UI can round-trip `installedConfig` without overwriting live secrets with the bullet string; admin-plugins.ts refactored to import the shared constant, dropping two local `const MASKED_PLACEHOLDER` definitions so a future drift can't break the write-path restore). **F-44 + F-45 shipped → PR #1831** — pino `serializers.err` now funnels every `err`-field value through the shared `errorMessage()` helper, and a new `formatters.log` walks top-level string fields for second-line defense against DSN userinfo in bystander fields (caller reason strings, serialized causes). `redactPaths` expanded with the F-44 field list: `cookie`, `set-cookie` (bracket-notation for fast-redact), `bearer`, `refreshToken`, `botToken`, `signingSecret`, `clientSecret`, `webhookSecret`, `appPassword`, `serverToken` — plus `*.FIELD`, `*[*].FIELD`, `[*].FIELD` variants so credential fields are redacted whether nested as object property, array-of-objects element, or root-array element. Local `errorMessage` / `causeToError` copies in `admin-scim.ts` / `admin-residency.ts` / `admin-roles.ts` consolidated to import from `@atlas/api/lib/audit/error-scrub` (direct module path, not the barrel — keeps existing partial-mock tests compatible). Call-site sweep: `admin-connections.ts` (17 log sites + 9 response-body interpolations found in review), `onboarding.ts` (8 sites), `db/connection.ts:1025` (health-check `scrubbedMessage` surfaces into `HealthCheckResult.message` so admin UI can't read a raw DSN out of the in-memory result, while the log line keeps `{type, message, stack}` via the serializer). Tests: new F-44 block in `logger.test.ts` covers serializer (string / Error / error-shape / truncation / fail-open), formatter (DSN scrub / non-match preservation / copy-on-write / allocation-free no-op / fail-open), 10 new field names + wildcard variants, and the F-44 repro asserting `hunter2` never appears in the emitted line for the probe-failure shape; `admin-connections.test.ts` adds a response-body regression block asserting the 400 JSON for a `healthCheck` rejection never carries the DSN password. Fail-open is enforced throughout: any scrubber exception emits a placeholder (or returns the original object for the formatter) rather than dropping the log — logs are forensic evidence, never sacrificed to scrub errors. Multi-agent review found: silent-failure-hunter H1/H2 (9 response-body sites echoing raw `err.message` → all routed through `errorMessage(err)` in fix commit so HTTP body + log line stay consistent), code-reviewer MEDIUM #1 (formatter mutated caller's object → copy-on-write), MEDIUM #2 (health-check log lost `{type, message, stack}` → restored). Incidental: #1829 filed to sweep the remaining ~550 `err instanceof Error ? err.message : String(err)` call sites across `packages/api/src` for code hygiene (not a security issue — the new serializer + formatter catch these) — shipped PR #1840, top-10 callers migrated to `errorMessage()`. **F-41 shipped → PR #1833** — workspace integration credentials encrypted at rest across all 10 install stores (Slack `bot_token`, Teams `app_password`, Discord `bot_token`, Telegram `bot_token`, GChat / GitHub credential blobs, Linear `api_key`, WhatsApp credentials, Email `config` JSONB, Sandbox `credentials` JSONB). New `packages/api/src/lib/db/secret-encryption.ts` exposes `encryptSecret` / `decryptSecret` wrappers around the existing `encryptUrl` AES-256-GCM helper (`iv:authTag:ciphertext` format) — JSONB blobs (email config, sandbox credentials) use blob-encryption (ciphertext-as-TEXT, decrypt → `JSON.parse` on read) rather than column-splitting so plugin-side shape invariants survive the round-trip. Migration `0036_integration_credentials_encryption.sql` adds `_encrypted` TEXT columns alongside every plaintext credential, keeps the plaintext columns dual-writing for soak; read-flip lands in the same PR so a lost encryption key doesn't silently null-out live integrations. One-shot backfill via `packages/api/src/lib/db/backfill-integration-credentials.ts` (237 lines, bun-runnable script; TypeScript not raw SQL because encryption happens in app code) encrypts existing rows post-migration with retry-safe idempotency (`WHERE ciphertext_col IS NULL`). Each store has a per-integration test file plus a `encryption-roundtrip.test.ts` pinning that plaintext → ciphertext → decrypted is byte-identical under a key-rotation simulation. Plaintext-drop is deferred to follow-up #1832 after one release soak — flipping reads and keeping dual-write for a release lets us roll back without data loss if the backfill misbehaves. Shared `packages/api/src/lib/plugins/secrets.ts` (already exported by F-43 for `MASKED_PLACEHOLDER` / `maskSecretFields`) gained `hasSecretKeys` + schema-walker reuse so plugin config encryption (F-42) can adopt the same helper without duplicating logic. **F-42 shipped → PR #1834** — plugin config secrets encrypted at rest across `plugin_settings.config` (platform-wide) + `workspace_plugins.config` (per-workspace marketplace installs). **Design choice: selective-field encryption inside the JSONB**, not the F-41 `*_encrypted` column split — the column is schemaless and secret-vs-non-secret is a property of the catalog schema, not the table. `packages/api/src/lib/plugins/secrets.ts` gained `encryptSecretFields` / `decryptSecretFields` / `isEncryptedSecret` walkers that share the three-state `ConfigSchema` shape with the existing F-43 `maskSecretFields` / `restoreMaskedSecrets` (absent → passthrough; parsed → encrypt/decrypt only `secret: true` keys; corrupt → fail-closed by encrypting every non-empty string). Non-secret operational fields (region, port, debug) stay as plain JSONB so DB ops keeps grep-ability; the disclosure surface shrinks to the actual credential values. Idempotent on `enc:v1:` prefix — the backfill and repeated PUTs are safely re-runnable. Write path (`admin-plugins.ts` PUT + `admin-marketplace.ts` POST /install + PUT /:id/config) decrypts originals → restore masked placeholders → re-encrypts before persisting, so every PUT refreshes the IV on preserved secrets (decrypt-then-reencrypt is the safer default over trusting idempotent ciphertext passthrough). Read responses mask back via `maskSecretFields` — the UI never sees ciphertext. Decryption failures surface as 500 with a `decryptFailure: true` audit row rather than silently handing null to the plugin runtime. Migration `0037_plugin_config_encryption.sql` stamps `COMMENT ON COLUMN` markers on both JSONB columns for audit-script discoverability (no schema change — the plaintext→ciphertext transition is intra-value). One-shot backfill via `packages/api/src/lib/db/backfill-plugin-config.ts` walks both carriers with a session-level advisory lock; `workspace_plugins` uses the catalog JOIN for per-row schema, `plugin_settings` falls back to fail-closed mode (no registry loaded) which is acceptable over-work for a one-time pass. `getPluginConfig()` in `plugins/settings.ts` gained a load-bearing comment pinning the decryption contract for future runtime consumers (hot-reload, per-request config swap). Tests: 21 walker unit tests (`secrets-encryption.test.ts`) + 9 backfill tests (`backfill-plugin-config.test.ts`) + 5 new route E2E tests in `admin-marketplace.test.ts` covering install/PUT encryption, mask-placeholder round-trip preserving prior ciphertext, response masking, and decrypt-failure audit + 500. Plaintext-drop **not** scheduled as a separate follow-up (unlike F-41 #1832): the plaintext→ciphertext transition is intra-value so there's no separate column to drop; soak-time plaintext-residue audit tracked as #1835. **F-46 shipped (#1819)** — bundled with F-29 residuals (see Phase 4 row) since both are audit-metadata hygiene on `admin-integrations.ts`. `hasSecret: true` landed on all 8 BYOT / OAuth-connect audit sites listed in the issue body (Slack/Teams/Discord BYOT, Telegram/GChat/GitHub/Linear/WhatsApp connect) plus the `POST /email` connect path (added during review — same credential-bearing shape). Additive metadata-shape change only — mirrors the F-30 precedent (`admin-email-provider.ts` + `admin-model-config.ts`); compliance queries filtering on `metadata.hasSecret` now catch every integration install / BYOT connect. `admin-integrations-audit.test.ts` pins the marker per-platform. Failure-path emissions added on all 9 save paths via `Effect.tapError` so store-throws (DB write failure, encryption failure, constraint violation) now land a `status: "failure"` row with `hasSecret: true` preserved — closes a pre-existing silent-failure gap surfaced in review. **F-47 shipped → PR #1837** — `ATLAS_ENCRYPTION_KEYS` (comma-separated `v<N>:raw` version prefixes) plus legacy single-key back-compat via `ATLAS_ENCRYPTION_KEY` / `BETTER_AUTH_SECRET` (treated as implicit v1). New `packages/api/src/lib/db/encryption-keys.ts` resolves the keyset; `encryptUrl` / `encryptSecret` now emit `enc:v<N>:iv:authTag:ciphertext` and decrypt counterparts parse the version prefix. Legacy un-versioned connection-URL ciphertext falls back to `byVersion.get(1) ?? active.key` so operators mid-rotation don't strand rows; warn breadcrumb fires on the active-fallback path. Migration `0038_encryption_key_versioning.sql` adds `*_key_version` to all 12 encrypted columns. Bun-runnable `packages/api/scripts/rotate-encryption-key.ts` walks every encrypted column, decrypts under any keyset entry, re-encrypts with the active key; exits `2` on orphans (dropped legacy keys). New `UnknownKeyVersionError` typed throw from `decryptSecret`; `pickDecryptedSecret` escalates to `log.error`. Startup warns under `ATLAS_DEPLOY_MODE=saas` when only `BETTER_AUTH_SECRET` is providing the key. Runbook at `apps/docs/content/docs/platform-ops/encryption-key-rotation.mdx` covers planned + emergency rotation flows with verification queries spanning all 12 encrypted columns. **Remaining in Phase 5:** none — all findings shipped.
- [x] Phase 6 — Audit rate limiting, timeouts, pool caps, DoS surfaces (#1725, PR #1855). 0 P0 / 1 P1 / 4 P2 / 4 P3-noted / 11 verified-clean; remediation queue #1844–#1848. ID range starts at F-73 to dodge Phase 7's parallel claim.
- [x] Phase 7 — Audit EE governance bypasses — SSO / SCIM / IP / approval (#1726, PR #1854). 0 P0 / 2 P1 / 3 P2 / 3 P3-noted; remediation queue #1849–#1853. Audit portion of 1.2.3 now closed; remaining work is the remediation queue + the two soak-deferred items (#1832, #1835).
- Remediation status (2026-04-25): 7/10 P1/P2 findings shipped — F-75/F-76 (PR #1856 webhook replay + per-channel rate limit), F-73/F-74/F-77 (PR #1861 rate-limit cluster + atomic per-conversation reservation), F-54/F-55 (PR #1860 scheduler + chat-platform user-binding). Open queue: F-53 (#1849 custom-role permission enforcement — multi-PR cluster), F-56 (#1852 SSO `simple-key`/`byot` gating), F-57 (#1853 SCIM provenance check), plus #1858 (MCP actor binding — filed during F-55 work, same root cause). Soak-deferred: #1832 (F-41 plaintext drop), #1835 (F-42 plaintext residue audit).

## 1.3.0 — End-User UI Design Pass

Revamp end-user facing surfaces using the treatment pattern from the 1.2.1/1.2.2 admin revamp (critique → distill → colorize → polish). No features, no backend. Tracking issue #1719. Starts after 1.2.3 closes; public-views bucket safe to pipeline in parallel.

- [ ] Bucket 1 — Chat (`/`) — message density, starter-prompt grid, tool calls, result cards, conversation sidebar
- [ ] Bucket 2 — Notebook (`/notebook`) — cell UX, fork-gutter verification, markdown cells, empty state
- [ ] Bucket 3 — Dashboards (`/dashboards`, `/dashboards/[id]`) — tile chrome, edit vs view, empty state, share affordance
- [ ] Bucket 4 — Public views (`/shared/[token]`, `/report/[token]`, embed) — branding, load perf, mobile, print — safe to pipeline with 1.2.3
- [ ] Bucket 5 — Onboarding (`/signup/*`, `/login`, `/create-org`, `/wizard`) — region picker, workspace create, success
- [ ] Bucket 6 — Demo (`/demo`) — the marketing hand-off surface

---

## Ideas / Backlog

_Untracked ideas. Create issues when committing to work._

### Expand Reach (build when demand signals appear)
- ~~Python SDK~~ — **closed** (#1181). No demand signal. Reopen when a Python user asks
- ~~MongoDB + GraphQL datasource plugins~~ — **closed** (#1182). Non-SQL needs major architecture work. No demand signal
- ~~Multi-seed selection in `create-atlas`~~ — **shipped** (#1188). Interactive seed picker (simple/cybersec/ecommerce), `--seed` flag, seed data restructured into `seeds/<name>/`

### Competitive Positioning
- ~~Benchmark participation~~ — **closed** (#1183). Lower priority post-launch. Revisit if needed for credibility
- ~~"Powered by Atlas" badge on embedded widgets~~ — **shipped** (PR #1265). Opt-out badge on @useatlas/react and script tag widget
- ~~OSI (Open Semantic Interchange) compatibility~~ — **closed** (#1184). Standard isn't stable yet. Adopt when it solidifies

### Product Extensions
- ~~Dashboard persistence~~ — **shipped** (#1246, PRs #1253–#1258). DB schema + CRUD API, add-to-dashboard from chat, list/view pages with DnD reorder, sharing + public view, auto-refresh via scheduler, AI-driven card suggestions
- Voice input / natural language voice queries — wait for Web Speech API maturity
- Multi-agent collaboration — specialist agents per domain with coordinator routing (#1178, deferred)
- ~~`atlas migrate`~~ — **shipped** (#1185, PR #1303). Snapshot, diff, log, rollback, auto-snapshot on `atlas improve` and `atlas init`
- ~~A/B testing for agent prompts~~ — **closed** (#1186). Needs replay infra, eval metrics, comparison UI. No demand signal

### MCP Enhancements
- WebSocket transport — enables real-time bidirectional communication
- ~~Prompt templates~~ — **shipped** (PR #1296). 5 built-in patterns + semantic layer query_patterns + prompt library
- Resource subscriptions — notify connected clients when semantic layer changes

### Plugin Ecosystem
- ~~Agent error recovery hooks~~ — **closed** (#1187). Speculative. Build when plugin authors request specific hooks
- ~~Streaming action approval~~ — **closed** (#1187). Same — build on demand
