---
title: Author a new starter prompt directly (skips pending queue)
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Creates a new `query_suggestions` row with `approval_status =
          'approved'` and `status = 'published'` so the admin can seed the empty
          state without waiting for organic engagement. Duplicate text (same
          `normalized_hash` in this org) returns 409 — approve or unhide the
          existing row instead.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Creates a new `query_suggestions` row with `approval_status = 'approved'` and `status = 'published'` so the admin can seed the empty state without waiting for organic engagement. Duplicate text (same `normalized_hash` in this org) returns 409 — approve or unhide the existing row instead.

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/admin/starter-prompts/author","method":"post"}]} />