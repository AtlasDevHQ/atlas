---
title: Export the whole dashboard as PNG or PDF
full: true
_openapi:
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Renders the full dashboard at the caller's current parameter values in
          a headless Chromium and returns the artifact as a downloadable
          attachment (filename = dashboard title + UTC timestamp). Reuses the
          screenshot pipeline (#2367) rather than a second headless path. A
          single tile that fails to render does NOT abort the export — the
          response carries `X-Atlas-Export-Partial: 1` and the partial board is
          still returned. `format` defaults to `pdf`. Requires admin role.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/dashboards/{id}/export","method":"post"}]} showDescription />