---
title: Per-user MFA factor snapshot
full: true
_openapi:
  method: GET
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Returns the calling user's password / TOTP / passkey-count snapshot
          used by the BackupMethodBanner on /admin/settings/security to detect
          the lockout-risk profile (one passkey, no password, no TOTP). Light
          auth — by definition the banner has to render before the user has a
          second factor enrolled.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Returns the calling user's password / TOTP / passkey-count snapshot used by the BackupMethodBanner on /admin/settings/security to detect the lockout-risk profile (one passkey, no password, no TOTP). Light auth — by definition the banner has to render before the user has a second factor enrolled.

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/admin/me/mfa-factors","method":"get"}]} />