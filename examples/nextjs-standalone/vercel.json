{
  "framework": "nextjs",
  "buildCommand": "cp -r ../../packages/cli/data/seeds/ecommerce/semantic semantic && bun run scripts/seed-demo.ts && bun run scripts/migrate-auth.ts && next build",
  "env": {
    "ATLAS_DEMO_DATA": "true"
  },
  "crons": [{ "path": "/api/v1/scheduled-tasks/tick", "schedule": "* * * * *" }]
}
