---
title: Hosted MCP server (SSE transport)
full: true
_openapi:
  method: GET
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Hosted Model Context Protocol endpoint. MCP clients (Claude Desktop,
          ChatGPT, Cursor) connect here over Server-Sent Events to call tools
          (`executeSQL`, `explore`, typed metric tools) and list prompts.
          Authentication is OAuth 2.1 + RFC 9728: clients first hit
          `/.well-known/oauth-protected-resource/mcp/{workspace_id}` to discover
          the authorization server, complete the OAuth flow, then connect here
          with `Authorization: Bearer <token>`. The token's `aud` claim must
          match the advertised resource URI and `ATLAS_OAUTH_WORKSPACE_CLAIM`
          must match `workspace_id`. Concurrent session count is bounded by
          `ATLAS_MCP_MAX_SESSIONS` (default 100); exceeding it returns `503
          too_many_sessions`. See the [MCP guide](/guides/mcp) for the full
          agent-onboarding flow.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Hosted Model Context Protocol endpoint. MCP clients (Claude Desktop, ChatGPT, Cursor) connect here over Server-Sent Events to call tools (`executeSQL`, `explore`, typed metric tools) and list prompts. Authentication is OAuth 2.1 + RFC 9728: clients first hit `/.well-known/oauth-protected-resource/mcp/{workspace_id}` to discover the authorization server, complete the OAuth flow, then connect here with `Authorization: Bearer <token>`. The token's `aud` claim must match the advertised resource URI and `ATLAS_OAUTH_WORKSPACE_CLAIM` must match `workspace_id`. Concurrent session count is bounded by `ATLAS_MCP_MAX_SESSIONS` (default 100); exceeding it returns `503 too_many_sessions`. See the [MCP guide](/guides/mcp) for the full agent-onboarding flow.

<APIPage document={"./openapi.json"} operations={[{"path":"/mcp/{workspace_id}/sse","method":"get"}]} />