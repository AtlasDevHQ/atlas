---
title: Submit form-based install (no OAuth)
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Persists the submitted credentials + install metadata for an
          `install_model: "form"` catalog entry (#2660 — Email, future Webhook /
          Obsidian). Validates the JSON body against the per-Platform schema,
          encrypts secret-marked fields at rest, and upserts the
          workspace_plugins row. 400 with field-level detail on validation
          failure; the route rejects requests pointed at non-form catalog
          entries with 400 (`wrong_install_model`).
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Persists the submitted credentials + install metadata for an `install_model: "form"` catalog entry (#2660 — Email, future Webhook / Obsidian). Validates the JSON body against the per-Platform schema, encrypts secret-marked fields at rest, and upserts the workspace_plugins row. 400 with field-level detail on validation failure; the route rejects requests pointed at non-form catalog entries with 400 (`wrong_install_model`).

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/integrations/{platform}/install-form","method":"post"}]} />