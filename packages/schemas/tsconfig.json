{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // #4955 — this package has graduated to exactOptionalPropertyTypes; the
    // enforcing gate is tsconfig.exact-optional.json in the root `type` script.
    // This copy is for editors, which pick the nearest tsconfig.
    "exactOptionalPropertyTypes": true
  },
  "include": ["src"]
}
