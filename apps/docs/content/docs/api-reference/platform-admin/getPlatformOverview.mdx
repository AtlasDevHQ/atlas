---
title: Deployment-wide overview
full: true
_openapi:
  method: GET
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Platform admin only. Returns deployment-scaffold counts (entities,
          plugins, plugin health) plus pool warnings. Component Health (the
          datasource pool, internal DB, LLM provider, scheduler, sandbox) is
          served from `/api/health` — kept there so unauthenticated readiness
          probes still have a target. The deployment-scaffold counts live here,
          away from the per-workspace `/admin/overview` (#2489).
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Platform admin only. Returns deployment-scaffold counts (entities, plugins, plugin health) plus pool warnings. Component Health (the datasource pool, internal DB, LLM provider, scheduler, sandbox) is served from `/api/health` — kept there so unauthenticated readiness probes still have a target. The deployment-scaffold counts live here, away from the per-workspace `/admin/overview` (#2489).

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/platform/overview","method":"get"}]} />