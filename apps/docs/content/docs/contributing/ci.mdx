---
title: CI Gates
description: GitHub Actions workflows that run on every Atlas pull request, plus the conventions a PR has to clear before it can merge.
---

> Reference for contributors. The authoritative list of pre-PR commands lives in [`CLAUDE.md`](https://github.com/AtlasDevHQ/atlas/blob/main/CLAUDE.md). This page documents the *workflows* that enforce them, plus a few that don't gate merge but still post results to the PR.

## Required gates (the `CI` workflow)

`.github/workflows/ci.yml` runs on every pull request to `main` and must pass before merge:

- **`bun install --frozen-lockfile`** — fails if `bun.lock` is out of date.
- **`bun x syncpack lint`** — dependency version consistency across the workspace.
- **Dockerfile bun version pins** — every `oven/bun:<version>` in any `Dockerfile` matches the workspace `BUN_VERSION`.
- **Dockerfile workspace completeness** — `scripts/check-dockerfile-workspace.sh`.
- **Railway watchPatterns coverage** — `scripts/check-railway-watch.sh`. Every `COPY` source in a deploy `Dockerfile` is covered by its service's `railway.json` watchPatterns. Prevents the F-10 incident where a `bun.lock` change went unshipped because `watchPatterns` didn't cover it.
- **Template drift** — `scripts/check-template-drift.sh`. `create-atlas/templates/nextjs-standalone/src/` is gitignored and regenerated by `prepare-templates.sh`; this check verifies it stays in sync.
- **Security headers drift** — `scripts/check-security-headers-drift.sh`.
- **Drizzle schema ↔ migration drift** — `scripts/check-schema-drift.sh`.
- **Builds** — SDKs, widget bundle, then the root build. The OpenAPI ↔ api-reference drift check runs after the builds because `extract-openapi.ts` needs the built dependencies at import time.
- **Type check** — `bun run type` (root) and `bun run --filter '@atlas/web' type`.
- **Lint** — `bun run lint`.
- **Test** — `@atlas/api` tests run in a parallel `api-tests` matrix sharded 4 ways. Other workspace test suites run serially in the main job.

The local equivalent is the `/ci` slash-command — same six gates as the workflow, run in one shot.

### Fast local feedback

The full `bun run test` suite is slow because `@atlas/api` runs every test file in its own subprocess (this avoids the cross-test state leaks that bit us before the isolated runner). For the edit/test cycle, prefer:

```bash
cd packages/api
bun run scripts/test-isolated.ts --affected     # only tests whose source graph your branch touched vs origin/main
bun run scripts/test-isolated.ts --since HEAD~3 # last-3-commit window
bun test path/to/file.test.ts                   # single file
```

Run the full `bun run test` once before opening a PR even if `--affected` has been clean — the `--affected` detector occasionally misses transitive imports.

## Lighthouse budget (advisory)

`.github/workflows/lighthouse.yml` runs on PRs that touch `apps/www/**`, `packages/web/**`, the `lighthouserc.js` config, or the workflow itself. It does **not** gate merge — the assertions in `lighthouserc.js` are all `warn`-level for the first month while we calibrate flake on shared CI runners.

What it does:

1. Builds `apps/www` (static export) and `packages/web` (Next prod build).
2. Starts both locally — apps/www on `:8080`, packages/web on `:3000`.
3. Runs Lighthouse 3× per surface, twice — once with `LH_FORM_FACTOR=desktop`, once `mobile`.
4. Uploads reports to:
   - The workflow artifact `lighthouse-reports` (HTML + JSON, 14-day retention).
   - Lighthouse CI's temporary public storage — shareable URLs in the report output.
5. Posts (or updates) a single PR comment with a per-surface score table.

### Surfaces covered

| Surface | Source | Captured baseline |
|---|---|---|
| `/` | `apps/www/src/app/page.tsx` | [`apps/www/.design/landing/lighthouse-baseline.md`](https://github.com/AtlasDevHQ/atlas/blob/main/apps/www/.design/landing/lighthouse-baseline.md) |
| `/pricing` | `apps/www/src/app/pricing/page.tsx` | [`apps/www/.design/pricing/lighthouse-baseline.md`](https://github.com/AtlasDevHQ/atlas/blob/main/apps/www/.design/pricing/lighthouse-baseline.md) |
| `/demo` (cold) | `packages/web/src/app/demo/page.tsx` | [`apps/www/.design/demo/lighthouse-baseline.md`](https://github.com/AtlasDevHQ/atlas/blob/main/apps/www/.design/demo/lighthouse-baseline.md) (#1945) |

`/demo` active state (post-gate, with a seeded sessionStorage bearer) is **not yet covered** — the active surface needs the API + DB stack at audit time, which is its own follow-up.

### Refreshing the baselines

The baseline `.md` files document what the surface measured at when CI was set up — they're the seed for `lighthouserc.js` thresholds, not a source of truth that CI re-asserts against. To re-measure (e.g., after a redesign):

```bash
cd apps/www
bun install
bun run build               # next build → apps/www/out/
PORT=8080 bun serve.ts &    # static server

# Hit http://localhost:8080/ and http://localhost:8080/pricing with the
# scratch runner pattern documented in
# apps/www/.design/demo/lighthouse-baseline.md.
```

Update the `.md` file with the new numbers and adjust the thresholds in `lighthouserc.js` to match. The thresholds should sit a hair below the WSL2 numbers — CI runners measure noisier than a dev box.

### Promoting to a hard gate

When an assertion has been stable on PR comments for ~4 weeks, change its level in `lighthouserc.js` from `"warn"` to `"error"`. lhci will then exit non-zero on a regression and the workflow's `continue-on-error: true` step setting will need to be reconsidered (right now it's there so a single flaky run doesn't bury the comment).
