---
title: Force-revoke all auth artifacts for a user
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Atomically deletes every active session, trusted-device cookie (and
          adjacent verification rows), passkey, and OAuth access/refresh token
          for the target user inside a single transaction. The user can
          re-enroll if re-hired; the contractor / compromised-account scenario
          is the load-bearing case. Workspace admins are scoped to their org
          members; platform admins cross-org.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Atomically deletes every active session, trusted-device cookie (and adjacent verification rows), passkey, and OAuth access/refresh token for the target user inside a single transaction. The user can re-enroll if re-hired; the contractor / compromised-account scenario is the load-bearing case. Workspace admins are scoped to their org members; platform admins cross-org.

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/admin/users/{id}/revoke-auth","method":"post"}]} />