---
title: Retry a dead outbox row
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          SaaS only. Flips a `dead` row back to `pending` and clears
          `last_error`. `attempts` is intentionally NOT reset so the
          deterministic backoff in lib/lead-outbox continues from where it left
          off — prevents an operator from foot-gunning infinite retries on a
          permanently-broken upstream call.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

SaaS only. Flips a `dead` row back to `pending` and clears `last_error`. `attempts` is intentionally NOT reset so the deterministic backoff in lib/lead-outbox continues from where it left off — prevents an operator from foot-gunning infinite retries on a permanently-broken upstream call.

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/platform/crm-outbox/{id}/retry","method":"post"}]} />