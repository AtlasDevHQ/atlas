name: DailyScanStats
type: fact_table
table: daily_scan_stats
grain: one row per daily scan stat record
description: >-
  Auto-profiled schema for daily_scan_stats (2,000 rows). Contains 9 columns, linked to organizations. DENORMALIZED —
  data may duplicate other tables.
dimensions:
  - name: id
    sql: id
    type: number
    description: Primary key
    primary_key: true
    unique_count: 2000
    sample_values:
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
      - '8'
  - name: organization_id
    sql: organization_id
    type: number
    unique_count: 200
    sample_values:
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
      - '8'
    profiler_notes:
      - Likely FK to organizations.id (inferred from column name, no constraint exists)
  - name: scan_date
    sql: scan_date
    type: date
    unique_count: 2000
    sample_values:
      - Sun Jan 01 2023 00:00:00 GMT-0500 (Eastern Standard Time)
      - Mon Jan 02 2023 00:00:00 GMT-0500 (Eastern Standard Time)
      - Tue Jan 03 2023 00:00:00 GMT-0500 (Eastern Standard Time)
      - Wed Jan 04 2023 00:00:00 GMT-0500 (Eastern Standard Time)
      - Thu Jan 05 2023 00:00:00 GMT-0500 (Eastern Standard Time)
      - Fri Jan 06 2023 00:00:00 GMT-0500 (Eastern Standard Time)
      - Sat Jan 07 2023 00:00:00 GMT-0500 (Eastern Standard Time)
      - Sun Jan 08 2023 00:00:00 GMT-0500 (Eastern Standard Time)
  - name: total_scans
    sql: total_scans
    type: number
    unique_count: 10
    sample_values:
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
      - '8'
  - name: total_findings
    sql: total_findings
    type: number
    unique_count: 100
    sample_values:
      - '0'
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
  - name: critical_count
    sql: critical_count
    type: number
    unique_count: 5
    sample_values:
      - '0'
      - '1'
      - '2'
      - '3'
      - '4'
  - name: high_count
    sql: high_count
    type: number
    unique_count: 20
    sample_values:
      - '0'
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
  - name: medium_count
    sql: medium_count
    type: number
    unique_count: 50
    sample_values:
      - '0'
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
  - name: low_count
    sql: low_count
    type: number
    unique_count: 30
    sample_values:
      - '0'
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
  - name: scan_date_year
    sql: EXTRACT(YEAR FROM scan_date)
    type: number
    description: Year extracted from scan_date
    virtual: true
  - name: scan_date_month
    sql: TO_CHAR(scan_date, 'YYYY-MM')
    type: string
    description: Year-month extracted from scan_date
    virtual: true
  - name: total_scans_bucket
    sql: |-
      CASE
        WHEN total_scans < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY total_scans) FROM daily_scan_stats) THEN 'Low'
        WHEN total_scans < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY total_scans) FROM daily_scan_stats) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: total scans bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: total_findings_bucket
    sql: |-
      CASE
        WHEN total_findings < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY total_findings) FROM daily_scan_stats) THEN 'Low'
        WHEN total_findings < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY total_findings) FROM daily_scan_stats) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: total findings bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: critical_count_bucket
    sql: |-
      CASE
        WHEN critical_count < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY critical_count) FROM daily_scan_stats) THEN 'Low'
        WHEN critical_count < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY critical_count) FROM daily_scan_stats) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: critical count bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: high_count_bucket
    sql: |-
      CASE
        WHEN high_count < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY high_count) FROM daily_scan_stats) THEN 'Low'
        WHEN high_count < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY high_count) FROM daily_scan_stats) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: high count bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: medium_count_bucket
    sql: |-
      CASE
        WHEN medium_count < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY medium_count) FROM daily_scan_stats) THEN 'Low'
        WHEN medium_count < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY medium_count) FROM daily_scan_stats) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: medium count bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: low_count_bucket
    sql: |-
      CASE
        WHEN low_count < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY low_count) FROM daily_scan_stats) THEN 'Low'
        WHEN low_count < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY low_count) FROM daily_scan_stats) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: low count bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
measures:
  - name: daily_scan_stat_count
    sql: id
    type: count_distinct
  - name: total_total_scans
    sql: total_scans
    type: sum
    description: Sum of total scans
  - name: avg_total_scans
    sql: total_scans
    type: avg
    description: Average total scans
  - name: total_total_findings
    sql: total_findings
    type: sum
    description: Sum of total findings
  - name: avg_total_findings
    sql: total_findings
    type: avg
    description: Average total findings
  - name: total_critical_count
    sql: critical_count
    type: sum
    description: Sum of critical count
  - name: avg_critical_count
    sql: critical_count
    type: avg
    description: Average critical count
  - name: total_high_count
    sql: high_count
    type: sum
    description: Sum of high count
  - name: avg_high_count
    sql: high_count
    type: avg
    description: Average high count
  - name: total_medium_count
    sql: medium_count
    type: sum
    description: Sum of medium count
  - name: avg_medium_count
    sql: medium_count
    type: avg
    description: Average medium count
  - name: total_low_count
    sql: low_count
    type: sum
    description: Sum of low count
  - name: avg_low_count
    sql: low_count
    type: avg
    description: Average low count
joins:
  - target_entity: Organizations
    relationship: many_to_one
    join_columns:
      from: organization_id
      to: id
    inferred: true
    note: No FK constraint exists — inferred from column name organization_id
    description: Each daily_scan_stat likely belongs to one organization
use_cases:
  - 'WARNING: This is a denormalized/materialized table. Data may be stale or duplicate other tables'
  - Use for aggregation and trends on total_scans, total_findings, critical_count, high_count, medium_count, low_count
  - Use for time-series analysis using scan_date
  - Join with organizations for cross-entity analysis
profiler_notes:
  - 'Possibly denormalized/materialized table: name matches reporting pattern. Data may duplicate other tables'
