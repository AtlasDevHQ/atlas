---
title: Archive connection group (cascade)
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Atomically marks the group `status = 'archived'` and cascades to every
          content row scoped to it: semantic entities → `archived`, scheduled
          tasks → `enabled = false`, pending approvals → `expired`. All four
          flips happen in one transaction — any sub-step failure rolls every
          flip back. Idempotent: re-archiving a group returns a 409 with the
          current archived state rather than double-counting.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Atomically marks the group `status = 'archived'` and cascades to every content row scoped to it: semantic entities → `archived`, scheduled tasks → `enabled = false`, pending approvals → `expired`. All four flips happen in one transaction — any sub-step failure rolls every flip back. Idempotent: re-archiving a group returns a 409 with the current archived state rather than double-counting.

<APIPage document={"./openapi.json"} operations={[{"path":"/api/v1/admin/connection-groups/{id}/archive","method":"post"}]} />