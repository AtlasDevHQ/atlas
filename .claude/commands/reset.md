Switch to the main branch, pull the latest changes, and install dependencies. Run these commands sequentially:

1. `git checkout main`
2. `git pull`
3. `bun install`

Report the result of each step concisely. If any step fails, stop and report the error.
