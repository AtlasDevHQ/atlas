---
title: SvelteKit
description: Integrate Atlas into a SvelteKit app using @ai-sdk/svelte.
---

import { Callout } from "fumadocs-ui/components/callout";

Integrate Atlas into a SvelteKit app using `@ai-sdk/svelte`.

> **Prerequisites:** A running Atlas API server. See [Bring Your Own Frontend](/docs/frameworks/overview) for architecture and common setup.

<Callout type="warn" title="AI SDK v6 compatibility">
  The transport-based API (`DefaultChatTransport`, `sendMessage`) shown below is specific to `@ai-sdk/react` and may not be available in `@ai-sdk/svelte`. If `useChat` from `@ai-sdk/svelte` does not support the `transport` option in your version, use the [sync query endpoint](/docs/frameworks/overview#streaming-vs-synchronous) (`POST /api/v1/query`) instead -- it works with any HTTP client and returns a structured JSON response. The [`@useatlas/sdk`](https://www.npmjs.com/package/@useatlas/sdk) package provides a typed client for this endpoint.
</Callout>

---

## 1. Install dependencies

```bash
bun add @ai-sdk/svelte ai
```

## 2. Configure the API URL

### Option A: Same-origin proxy (recommended)

In `vite.config.ts`, proxy `/api` to the Atlas API during development:

```typescript
// vite.config.ts
import { sveltekit } from "@sveltejs/kit/vite";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [sveltekit()],
  server: {
    proxy: {
      "/api": {
        target: "http://localhost:3001",
        changeOrigin: true,
      },
    },
  },
});
```

### Option B: Cross-origin

```bash
# .env
PUBLIC_ATLAS_API_URL=http://localhost:3001
```

```bash
# Atlas API .env
ATLAS_CORS_ORIGIN=http://localhost:5173
```

## 3. Chat store

Create a reusable chat module using `@ai-sdk/svelte`'s `useChat`:

```typescript
// src/lib/atlas-chat.ts
import { useChat } from "@ai-sdk/svelte";
import { DefaultChatTransport } from "ai";
import { env } from "$env/dynamic/public";

const apiUrl = env.PUBLIC_ATLAS_API_URL ?? "";

export function createAtlasChat(apiKey: () => string) {
  const transport = new DefaultChatTransport({
    api: `${apiUrl}/api/chat`,
    get headers() {
      const key = apiKey();
      const h: Record<string, string> = {};
      if (key) h["Authorization"] = `Bearer ${key}`;
      return h;
    },
  });

  const { messages, sendMessage, status, error } = useChat({ transport });

  return { messages, sendMessage, status, error };
}
```

## 4. Rendering tool calls

See the [React/Vite guide](/docs/frameworks/react-vite#rendering-tool-calls) for a complete `ToolPart` implementation. The data shapes are identical -- adapt the JSX to Svelte component syntax. The key shapes:

- **`executeSQL` output** -- `{ success, columns, rows, row_count, truncated }` on success, or `{ success: false, error }` on failure.
- **`explore` output** -- a plain string (stdout of the command).
- **`state`** -- check for `"output-available"` to know when the result is ready.

See the [Data Stream Protocol](/docs/frameworks/overview#data-stream-protocol) section for the full part structure and a concrete example.

## 5. Synchronous queries (alternative)

If streaming is not needed, use the JSON query endpoint:

```typescript
// src/lib/atlas-query.ts
import { env } from "$env/dynamic/public";

const apiUrl = env.PUBLIC_ATLAS_API_URL ?? "";

export async function queryAtlas(question: string, apiKey?: string) {
  const headers: Record<string, string> = {
    "Content-Type": "application/json",
  };
  if (apiKey) headers["Authorization"] = `Bearer ${apiKey}`;

  const res = await fetch(`${apiUrl}/api/v1/query`, {
    method: "POST",
    headers,
    body: JSON.stringify({ question }),
  });

  if (!res.ok) throw new Error(`Atlas query failed: ${res.status}`);
  return res.json();
}
```

See [Bring Your Own Frontend](/docs/frameworks/overview) for the full architecture and what `@atlas/web` adds on top.
