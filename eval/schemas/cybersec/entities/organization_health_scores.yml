name: OrganizationHealthScores
type: fact_table
table: organization_health_scores
grain: one row per organization health score record
description: Auto-profiled schema for organization_health_scores (200 rows). Contains 7 columns, linked to organizations.
dimensions:
  - name: id
    sql: id
    type: number
    description: Primary key
    primary_key: true
    unique_count: 200
    sample_values:
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
      - '8'
  - name: organization_id
    sql: organization_id
    type: number
    unique_count: 200
    sample_values:
      - '1'
      - '2'
      - '3'
      - '4'
      - '5'
      - '6'
      - '7'
      - '8'
    profiler_notes:
      - Likely FK to organizations.id (inferred from column name, no constraint exists)
  - name: score
    sql: score
    type: number
    unique_count: 161
    sample_values:
      - '50.3'
      - '50.5'
      - '50.6'
      - '50.8'
      - '50.9'
      - '51'
      - '51.1'
      - '51.5'
  - name: vulnerability_score
    sql: vulnerability_score
    type: number
    unique_count: 175
    sample_values:
      - '30.1'
      - '30.7'
      - '32'
      - '32.8'
      - '33.4'
      - '33.9'
      - '34.3'
      - '34.4'
  - name: compliance_score
    sql: compliance_score
    type: number
    unique_count: 173
    sample_values:
      - '40'
      - '40.1'
      - '40.3'
      - '40.6'
      - '40.7'
      - '41.3'
      - '42'
      - '42.1'
  - name: asset_coverage_score
    sql: asset_coverage_score
    type: number
    unique_count: 160
    sample_values:
      - '60.2'
      - '60.3'
      - '61'
      - '61.1'
      - '61.3'
      - '61.5'
      - '61.7'
      - '62.3'
  - name: calculated_at
    sql: calculated_at
    type: date
    unique_count: 200
    sample_values:
      - Tue Feb 24 2026 13:52:58 GMT-0500 (Eastern Standard Time)
      - Tue Feb 24 2026 14:05:50 GMT-0500 (Eastern Standard Time)
      - Tue Feb 24 2026 14:46:09 GMT-0500 (Eastern Standard Time)
      - Tue Feb 24 2026 14:53:27 GMT-0500 (Eastern Standard Time)
      - Tue Feb 24 2026 16:30:34 GMT-0500 (Eastern Standard Time)
      - Tue Feb 24 2026 16:35:14 GMT-0500 (Eastern Standard Time)
      - Tue Feb 24 2026 16:47:04 GMT-0500 (Eastern Standard Time)
      - Tue Feb 24 2026 17:25:38 GMT-0500 (Eastern Standard Time)
  - name: score_bucket
    sql: |-
      CASE
        WHEN score < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY score) FROM organization_health_scores) THEN 'Low'
        WHEN score < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY score) FROM organization_health_scores) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: score bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: vulnerability_score_bucket
    sql: |-
      CASE
        WHEN vulnerability_score < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY vulnerability_score) FROM organization_health_scores) THEN 'Low'
        WHEN vulnerability_score < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY vulnerability_score) FROM organization_health_scores) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: vulnerability score bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: compliance_score_bucket
    sql: |-
      CASE
        WHEN compliance_score < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY compliance_score) FROM organization_health_scores) THEN 'Low'
        WHEN compliance_score < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY compliance_score) FROM organization_health_scores) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: compliance score bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: asset_coverage_score_bucket
    sql: |-
      CASE
        WHEN asset_coverage_score < (SELECT PERCENTILE_CONT(0.33) WITHIN GROUP (ORDER BY asset_coverage_score) FROM organization_health_scores) THEN 'Low'
        WHEN asset_coverage_score < (SELECT PERCENTILE_CONT(0.66) WITHIN GROUP (ORDER BY asset_coverage_score) FROM organization_health_scores) THEN 'Medium'
        ELSE 'High'
      END
    type: string
    description: asset coverage score bucketed into Low/Medium/High terciles
    virtual: true
    sample_values:
      - Low
      - Medium
      - High
  - name: calculated_at_year
    sql: EXTRACT(YEAR FROM calculated_at)
    type: number
    description: Year extracted from calculated_at
    virtual: true
  - name: calculated_at_month
    sql: TO_CHAR(calculated_at, 'YYYY-MM')
    type: string
    description: Year-month extracted from calculated_at
    virtual: true
measures:
  - name: organization_health_score_count
    sql: id
    type: count_distinct
  - name: total_score
    sql: score
    type: sum
    description: Sum of score
  - name: avg_score
    sql: score
    type: avg
    description: Average score
  - name: total_vulnerability_score
    sql: vulnerability_score
    type: sum
    description: Sum of vulnerability score
  - name: avg_vulnerability_score
    sql: vulnerability_score
    type: avg
    description: Average vulnerability score
  - name: total_compliance_score
    sql: compliance_score
    type: sum
    description: Sum of compliance score
  - name: avg_compliance_score
    sql: compliance_score
    type: avg
    description: Average compliance score
  - name: total_asset_coverage_score
    sql: asset_coverage_score
    type: sum
    description: Sum of asset coverage score
  - name: avg_asset_coverage_score
    sql: asset_coverage_score
    type: avg
    description: Average asset coverage score
joins:
  - target_entity: Organizations
    relationship: many_to_one
    join_columns:
      from: organization_id
      to: id
    inferred: true
    note: No FK constraint exists — inferred from column name organization_id
    description: Each organization_health_score likely belongs to one organization
use_cases:
  - Use for aggregation and trends on score, vulnerability_score, compliance_score, asset_coverage_score
  - Use for time-series analysis using calculated_at
  - Join with organizations for cross-entity analysis
